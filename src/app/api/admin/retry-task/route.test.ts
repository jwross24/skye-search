/**
 * Retry-task route tests live in the consolidated admin test file.
 * This file exists to satisfy the co-location check (check-test-coverage.sh).
 *
 * @see ../admin-routes.test.ts → "POST /api/admin/retry-task"
 */

export { } // ensure this is treated as a module
